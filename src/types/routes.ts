export const enum RoutePath {
  Home = "/home",
  Portfolios = "/portfolios",
  Quote = "/quote",
  Calculator = "/calc",
  Portfolio = "/portfolio/:id",
  Login = "/login",
  Register = "/register",
  CatchAll = "/*",
}
